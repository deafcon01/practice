apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: nginx
    env: sandbox
spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: NGINX_PORT
          value: "8080"
